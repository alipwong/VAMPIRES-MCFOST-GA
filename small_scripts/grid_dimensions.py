# This script is used to investigate the outputs from mcfost when running with the -output_density_grid flag.
# In particular grid.fits and dust_mass_density.fits

# from astropy.io import fits
# fits_image_filename = fits.util.get_testdata_filepath('../data_disk/grid.fits')
# hdul = fits.open(fits_image_filename)
